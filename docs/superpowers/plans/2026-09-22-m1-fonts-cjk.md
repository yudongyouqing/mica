# M1-A 字体与中文 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 DirectWrite 真字体替换 font8x8 位图,让 Mica 显示任意文字(含中文,宽字符对齐正确),格子尺寸由字体度量驱动。

**Architecture:** `mica-render` 新增 `font/` 模块(D8):纯逻辑层(style/宽度判定、度量数学、图集排布、字形路由 trait)+ `#[cfg(windows)]` 的 DirectWrite 光栅化实现。`CellInstance` 48B→64B,每实例自带像素尺寸与图集 UV 矩形;shader 简化为纯矩形绘制。app 接线换源。

**Tech Stack:** windows-rs 0.62(`Win32_Graphics_DirectWrite` feature,已核实 0.62.2 签名)、unicode-width(格子宽度判定,East Asian Width 标准)、现有 wgpu 30 / alacritty_terminal 0.26。

**Spec:** `docs/superpowers/specs/2026-09-22-m1-render-config-design.md`(§3 字体管线、§4 渲染层;本计划覆盖切片 M1a+M1b,M1c+M1d 为后续计划 B)。

## Global Constraints

- **DirectWrite 已核实事实(0.62.2,全部逐字验证)**:`DWriteCreateFactory::<IDWriteFactory>(DWRITE_FACTORY_TYPE_SHARED)` 泛型按接口;`GetSystemFontCollection(*mut Option<IDWriteFontCollection>, bool)`/`FindFamilyName(P0, *mut u32, *mut BOOL)`/`GetMetrics(*mut DWRITE_FONT_METRICS)` 都是**裸指针 out 参数**;`DWRITE_GLYPH_RUN.fontFace: ManuallyDrop<Option<IDWriteFontFace>>` 要 `ManuallyDrop::new(Some(face))`;`CreateAlphaTexture(texture, *const RECT, &mut [u8])` slice 长度即 bufferSize,`TEXTURE_ALIASED_1x1` = 每像素 1 字节灰度;`GetDesignGlyphMetrics(*const u16, u32, *mut DWRITE_GLYPH_METRICS, bool)`;像素换算 `px = design_units as f32 * em_size / designUnitsPerEm as f32`,`fontEmSize` 单位 DIP(pt × 96/72),默认 12pt = 16.0 DIP。
- **alacritty 已核实**:`term.mode()`/`TermMode::APP_CURSOR`、`grid.display_offset()` 是方法、cell Flags 精确拼写 `WIDE_CHAR`/`WIDE_CHAR_SPACER`/`BOLD`/`ITALIC`(`cell.flags` 是 pub 字段)、**damage 迭代器产出即视口行号**(0=视口顶,无需换算)。
- **消息循环已核实**:`GetMessageW` 返回 BOOL,**-1 是错误**(`.0 as i32 == -1` 判断,不能当 true);`PostMessageW(Option<HWND>, u32, WPARAM, LPARAM) -> Result<()>`。
- 版本钉死:新增 `unicode-width = "0.2"`(mica-render)、mica-render 的 `[target.'cfg(windows)'.dependencies]` 加 `windows`(workspace 版,features 在 mica-render 侧追加 `Win32_Graphics_DirectWrite`)。若 unicode-width 0.2 实际 API 有出入以编译器为准(只用到 `UnicodeWidthChar::width`)。
- mac 开发主循环:纯逻辑任务全 TDD;windows-only 代码(dwrite、app 接线)用 `cargo clippy --target x86_64-pc-windows-msvc` 交叉验证,**运行冒烟必须真机**。
- M1-A 边界(不做,勿顺手实现):配置/主题(计划 B)、调色板收敛(B)、DECCKM(B)、事件化(B)、连字(M2)、COLR 彩色 emoji(M2,先灰度)、脏区跟踪(B——本计划 build 仍全量)。
- 代码纪律:每文件单一职责;测试先行;conventional commits;license MIT OR Apache-2.0,禁 GPL。
- 现有消费方提醒:`surface::CELL_WIDTH/CELL_HEIGHT`(u16)与 `atlas::CELL_WIDTH/CELL_HEIGHT`(u32)+ 编译期断言在本计划 T2 退役;`frame::CellInstance` 48B 布局测试在 T4 改 64B;WGSL `cells.wgsl` 在 T4 重写。

---

### Task 1: font 模块——字形样式与字符宽度(纯逻辑)

**Files:**
- Create: `crates/mica-render/src/font/mod.rs`
- Modify: `crates/mica-render/src/lib.rs`(追加 `pub mod font;`)
- Modify: `crates/mica-render/Cargo.toml`(加 `unicode-width`)
- Modify: 根 `Cargo.toml` `[workspace.dependencies]`

**Interfaces:**
- Consumes: `alacritty_terminal::term::cell::Flags`(已核实拼写)
- Produces:
  - `font::GlyphStyle { bold: bool, italic: bool }` + `GlyphStyle::PLAIN` + `GlyphStyle::from_flags(flags: Flags) -> Self`(读 BOLD/ITALIC,忽略其它位)
  - `font::char_width(ch: char) -> usize`(1 或 2,East Asian Width)
  - `font::DEFAULT_FAMILIES: &[&str]` = `["Sarasa Mono SC", "Cascadia Mono", "Microsoft YaHei", "Segoe UI Emoji"]`(D9)

- [ ] **Step 1: 加依赖**

根 `Cargo.toml` `[workspace.dependencies]` 追加一行:

```toml
unicode-width = "0.2"
```

`crates/mica-render/Cargo.toml` `[dependencies]` 追加:

```toml
unicode-width = { workspace = true }
```

- [ ] **Step 2: 写失败测试**

`crates/mica-render/src/font/mod.rs`:

```rust
//! Font pipeline: style/width policy (pure), metrics math, atlas, routing.
//! The DirectWrite rasterizer lives in `dwrite` behind #[cfg(windows)].

#[cfg(test)]
mod tests {
    use super::*;
    use alacritty_terminal::term::cell::Flags;

    #[test]
    fn plain_and_flag_mapping() {
        assert_eq!(GlyphStyle::PLAIN, GlyphStyle { bold: false, italic: false });
        assert_eq!(GlyphStyle::from_flags(Flags::BOLD), GlyphStyle { bold: true, italic: false });
        assert_eq!(GlyphStyle::from_flags(Flags::ITALIC), GlyphStyle { bold: false, italic: true });
        assert_eq!(
            GlyphStyle::from_flags(Flags::BOLD_ITALIC),
            GlyphStyle { bold: true, italic: true }
        );
        // 无关位(如 INVERSE)不影响样式
        assert_eq!(GlyphStyle::from_flags(Flags::INVERSE), GlyphStyle::PLAIN);
    }

    #[test]
    fn ascii_is_one_cell_wide() {
        for ch in "aZ0 ~!@#".chars() {
            assert_eq!(char_width(ch), 1, "{ch:?}");
        }
    }

    #[test]
    fn cjk_ideographs_and_fullwidth_are_two_cells() {
        for ch in ['中', '文', '字', 'あ', 'ア', '한', '。', '，'] {
            assert_eq!(char_width(ch), 2, "{ch:?}");
        }
    }

    #[test]
    fn emoji_width_follows_unicode_width_table() {
        // U+1F600 在 unicode-width 0.2 的 East Asian Width 判定为宽(2)
        assert_eq!(char_width('\u{1F600}'), 2);
        // 组合用区/零宽字符为 0,按 1 格兜底(不产生 0 宽实例)
        assert_eq!(char_width('\u{200D}'), 1);
    }

    #[test]
    fn default_families_lead_with_sarasa() {
        assert_eq!(DEFAULT_FAMILIES[0], "Sarasa Mono SC");
        assert!(DEFAULT_FAMILIES.contains(&"Segoe UI Emoji"));
    }
}
```

`lib.rs` 追加 `pub mod font;`。此步只有测试,编译失败(T1 空模块)——为让"红"是断言红而非编译错,先放类型空壳:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct GlyphStyle { pub bold: bool, pub italic: bool }

impl GlyphStyle {
    pub const PLAIN: Self = Self { bold: false, italic: false };
}

pub fn char_width(_ch: char) -> usize { 1 } // 故意错值让 CJK 测试红

pub const DEFAULT_FAMILIES: &[&str] = &["Sarasa Mono SC"];
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cargo test -p mica-render font`
Expected: `cjk_ideographs_and_fullwidth_are_two_cells`、`emoji_width_follows_unicode_width_table`、`default_families_lead_with_sarasa` FAIL;样式测试 PASS(空壳已对)。

- [ ] **Step 4: 实现**

```rust
use alacritty_terminal::term::cell::Flags;
use unicode_width::UnicodeWidthChar;

/// 一个字形的呈现变体(来自 cell flags 的样式位)。
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Hash)]
pub struct GlyphStyle {
    pub bold: bool,
    pub italic: bool,
}

impl GlyphStyle {
    pub const PLAIN: Self = Self { bold: false, italic: false };

    pub fn from_flags(flags: Flags) -> Self {
        Self { bold: flags.contains(Flags::BOLD), italic: flags.contains(Flags::ITALIC) }
    }
}

/// 字符占的格子数(East Asian Width:宽/全角 = 2)。
/// 零宽字符(组合符/ZWJ)按 1 兜底,保证实例总是可绘制的。
pub fn char_width(ch: char) -> usize {
    ch.width().filter(|w| *w > 0).unwrap_or(1)
}

/// 默认字体回退链(D9):更纱黑体严格等宽中英混排,未安装时落系统链。
pub const DEFAULT_FAMILIES: &[&str] =
    &["Sarasa Mono SC", "Cascadia Mono", "Microsoft YaHei", "Segoe UI Emoji"];
```

(测试模块保留在文件尾部;`from_flags` 实现后样式测试继续绿。)

- [ ] **Step 5: 跑测试确认全绿 + 提交**

Run: `cargo test -p mica-render font && cargo fmt --all && cargo clippy -p mica-render --all-targets -- -D warnings`
Expected: 5 个 font 测试 PASS,套件其余 16+ 不受影响。

```bash
git add Cargo.toml Cargo.lock crates/mica-render/
git commit -m "feat(font): glyph style from cell flags, east-asian width, default family chain"
```

---

### Task 2: FontMetrics——字体度量驱动格子,硬编码常量退役

**Files:**
- Create: `crates/mica-render/src/font/metrics.rs`
- Modify: `crates/mica-render/src/font/mod.rs`(追加 `pub mod metrics;`)
- Modify: `crates/mica-core/src/surface.rs`(CELL_* 退役 → 可设置的度量)
- Modify: `crates/mica-render/src/frame.rs`(pos 计算改用度量参数)
- Modify: `crates/mica-app/src/app.rs`(窗口/网格换算改用度量;交叉验证)
- Delete: `crates/mica-render/src/atlas.rs` 中 `CELL_WIDTH/CELL_HEIGHT` 与编译期断言(该文件在 T4 整体重写,此处先删常量引用处)——**实际做法:本任务把 frame.rs/pipeline.rs/app.rs 对 CELL_* 的引用全部改为运行时度量,atlas.rs 保持原样不再被引用,T4 重写时删除。**

**Interfaces:**
- Consumes: 无新外部
- Produces:
  - `font::metrics::FontMetrics { cell_width: f32, line_height: f32, ascent: f32, descent: f32 }`
  - `FontMetrics::from_dwrite(ascent: u16, descent: u16, line_gap: i16, design_units_per_em: u16, em_size_dip: f32, max_advance_du: u32) -> FontMetrics`(纯数学,已核实换算式)
  - `mica_core::surface::Surface::set_cell_metrics(&mut self, cell_width: u16, cell_height: u16)`(WindowSize 查询应答用;`Surface::new` 默认按 8×16,启动后由 app 立即设置真值)
  - 后续任务以 `metrics.cell_width/cell_height` 作为网格换算唯一来源

- [ ] **Step 1: 写失败测试(metrics.rs)**

```rust
//! 由字体设计度量推格子像素尺寸(纯数学,DWRITE 换算已核实:
//! px = design_units * em_size_dip / designUnitsPerEm)。

#[cfg(test)]
mod tests {
    use super::*;

    // 假想字体:1000 units/em,ascent 800,descent 200,lineGap 0,最大 advance 500
    // em = 16 DIP(12pt)→ 1 du = 0.016 px
    #[test]
    fn converts_design_units_to_pixels() {
        let m = FontMetrics::from_dwrite(800, 200, 0, 1000, 16.0, 500);
        assert!((m.ascent - 12.8).abs() < 1e-4, "{}", m.ascent);
        assert!((m.descent - 3.2).abs() < 1e-4);
        assert!((m.line_height - 16.0).abs() < 1e-4);
        assert!((m.cell_width - 8.0).abs() < 1e-4, "500du * 0.016");
    }

    #[test]
    fn line_height_includes_gap() {
        let m = FontMetrics::from_dwrite(800, 200, 100, 1000, 16.0, 500);
        assert!((m.line_height - 17.6).abs() < 1e-4);
    }

    #[test]
    fn cell_width_never_below_one_pixel() {
        let m = FontMetrics::from_dwrite(10, 5, 0, 65535, 1.0, 1);
        assert!(m.cell_width >= 1.0);
        assert!(m.line_height >= 1.0);
    }
}
```

- [ ] **Step 2: 跑红 → 实现**

```rust
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct FontMetrics {
    pub cell_width: f32,
    pub line_height: f32,
    pub ascent: f32,
    pub descent: f32,
}

impl FontMetrics {
    /// design-unit 度量 → 像素。`max_advance_du` 为主字体最大 advance(等宽字体即单一 advance)。
    pub fn from_dwrite(
        ascent: u16,
        descent: u16,
        line_gap: i16,
        design_units_per_em: u16,
        em_size_dip: f32,
        max_advance_du: u32,
    ) -> Self {
        let dpuem = design_units_per_em.max(1) as f32;
        let px = |du: f32| du * em_size_dip / dpuem;
        let ascent = px(ascent as f32);
        let descent = px(descent as f32);
        let line_height = (ascent + descent + px(line_gap as f32)).max(1.0);
        let cell_width = px(max_advance_du as f32).max(1.0);
        Self { cell_width, line_height, ascent, descent }
    }
}
```

Run: `cargo test -p mica-render metrics` → 3 PASS。

- [ ] **Step 3: Surface 增度量设置,常量退役**

`crates/mica-core/src/surface.rs`:
- 删除 `pub const CELL_WIDTH/CELL_HEIGHT`(及其 doc)
- `ProxyState` 增 `cell: (u16, u16)` 默认 `(8, 16)`;新增:

```rust
    /// 查询应答用的格子像素尺寸(dwrite 度量产出自 mica-render,启动后立即设置真值)
    pub fn set_cell_metrics(&mut self, cell_width: u16, cell_height: u16) {
        self.proxy.0.lock().unwrap_or_else(|e| e.into_inner()).cell = (cell_width, cell_height);
    }
```

- `send_event` 的 `TextAreaSizeRequest` 分支用 `state.cell` 替代常量
- `mica-render/src/frame.rs`:`build_instances(surface: &Surface, metrics: &FontMetrics) -> Vec<CellInstance>`——`pos_glyph` 的 x/y 用 `col as f32 * metrics.cell_width` / `line as f32 * metrics.line_height`;调用方(测试)传 `&FontMetrics { cell_width: 8.0, line_height: 16.0, ascent: 12.0, descent: 4.0 }` 保持现有断言数值不变(测试逐个更新签名)
- `pipeline.rs` `Globals.cell` 改由 `Renderer::new` 参数 `cell: [f32; 2]` 传入(暂由调用方用 8.0/16.0,app 接线在 T7 换真值);`draw` 里 per-frame Globals 同步用构造时的 cell
- `mica-app/src/app.rs`:窗口初始尺寸与 WM_SIZE 网格换算改用 `metrics.cell_width/line_height`(来源暂为 `FontMetrics { 8.0, 16.0, .. }` 占位常量 `FALLBACK_METRICS`,T7 换 dwrite 真值);`surface.set_cell_metrics(w, h)` 在 init 与 resize 时调用
- `atlas.rs` 里的 `CELL_WIDTH/CELL_HEIGHT` 与 `const _: () = assert!` 删除该文件内对 `mica_core::surface::CELL_*` 的引用(直接删除断言块;文件 T4 重写)

- [ ] **Step 4: 全量验证(mac + 交叉)**

Run: `cargo test --workspace && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo clippy -p mica-app --target x86_64-pc-windows-msvc --all-targets -- -D warnings`
Expected: 全绿(frame 测试更新后数值不变,只是签名多了 metrics 参数)。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(font): FontMetrics from dwrite design units; retire hardcoded cell consts"
```

---

### Task 3: GlyphAtlas v2——变宽字形排布(纯逻辑)

**Files:**
- Create: `crates/mica-render/src/font/atlas.rs`(替代旧 `crates/mica-render/src/atlas.rs`,后者 T4 删)
- Modify: `crates/mica-render/src/font/mod.rs`(追加 `pub mod atlas;`)

**Interfaces:**
- Consumes: 无
- Produces:
  - `font::atlas::GlyphBitmap { width: u32, height: u32, pixels: Vec<u8> }`(R8,调用方填充)
  - `font::atlas::Rect { u: u32, v: u32, w: u32, h: u32 }`(图集像素坐标;T4/T5 换算 0..1 UV)
  - `font::atlas::GlyphAtlas::new(width: u32, height: u32) -> Self`
  - `GlyphAtlas::insert(&mut self, bmp: &GlyphBitmap) -> Rect`(shelf 分配;**放不下时自动倍增高×2 并重排既有条目,返回的新 Rect 即重排后位置**)
  - `GlyphAtlas::width() -> u32`、`height() -> u32`、`texture(&self) -> &[u8]`(R8 连续行,无 padding——wgpu 纹理上传走 `COPY_DST` 整图重建,不用 buffer 行距对齐路径则无 256 对齐问题:直接 `queue.write_texture` 整块,行距 = width,合法)
  - `GlyphAtlas::version() -> u64`(每次重排 +1;调用方据此重传纹理)

- [ ] **Step 1: 写失败测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn bmp(w: u32, h: u32, tag: u8) -> GlyphBitmap {
        GlyphBitmap { width: w, height: h, pixels: vec![tag; (w * h) as usize] }
    }

    #[test]
    fn inserts_do_not_overlap() {
        let mut a = GlyphAtlas::new(128, 128);
        let r1 = a.insert(&bmp(10, 10, 1));
        let r2 = a.insert(&bmp(20, 12, 2));
        let r3 = a.insert(&bmp(5, 40, 3));
        for (ra, rb) in [(r1, r2), (r1, r3), (r2, r3)] {
            assert!(
                ra.u + ra.w <= rb.u || rb.u + rb.w <= ra.u || ra.v + ra.h <= rb.v || rb.v + rb.h <= ra.v,
                "{ra:?} overlaps {rb:?}"
            );
        }
        assert_eq!(a.texture().len(), (a.width() * a.height()) as usize);
    }

    #[test]
    fn pixels_land_at_their_rect() {
        let mut a = GlyphAtlas::new(64, 64);
        let r = a.insert(&bmp(3, 2, 7));
        for y in 0..r.h {
            for x in 0..r.w {
                assert_eq!(a.texture()[((r.v + y) * a.width() + r.u + x) as usize], 7);
            }
        }
    }

    #[test]
    fn growth_repacks_and_preserves_pixels() {
        let mut a = GlyphAtlas::new(32, 32);
        let r1 = a.insert(&bmp(16, 16, 9));
        assert_eq!(a.version(), 0);
        // 高度 30 的字形在 32 高图集放不下 → 倍增重排
        let r2 = a.insert(&bmp(16, 30, 5));
        assert_eq!(a.height(), 64);
        assert!(a.version() >= 1);
        // 两个矩形的内容都还在(位置可能变了)
        for (r, tag) in [(r1, 9u8), (r2, 5u8)] {
            for y in 0..r.h {
                for x in 0..r.w {
                    assert_eq!(a.texture()[((r.v + y) * a.width() + r.u + x) as usize], tag, "{r:?} tag{tag}");
                }
            }
        }
    }

    #[test]
    fn many_inserts_stay_consistent() {
        let mut a = GlyphAtlas::new(64, 64);
        let mut rects = Vec::new();
        for i in 0..200u8 {
            rects.push(a.insert(&bmp((i % 7 + 2) as u32, (i % 5 + 2) as u32, i)));
        }
        assert_eq!(a.texture().len(), (a.width() * a.height()) as usize);
        for (i, r) in rects.iter().enumerate() {
            assert!(r.u + r.w <= a.width() && r.v + r.h <= a.height(), "#{i} {r:?}");
        }
    }
}
```

- [ ] **Step 2: 跑红(空壳:`GlyphAtlas` 只有字段与 `todo!()` 方法)→ Step 3: 实现**

```rust
//! 字形图集:R8 纹素、shelf 行分配、放不下自动倍增高并重排。
//! 仅在主线程访问(渲染路径),无锁。

/// 待排布的字形位图(R8,每像素 1 字节灰度)。
pub struct GlyphBitmap {
    pub width: u32,
    pub height: u32,
    pub pixels: Vec<u8>,
}

/// 图集内矩形(像素坐标,UV 换算由消费方做)。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Rect {
    pub u: u32,
    pub v: u32,
    pub w: u32,
    pub h: u32,
}

struct Shelf {
    y: u32,
    height: u32,
    used_x: u32,
}

pub struct GlyphAtlas {
    width: u32,
    height: u32,
    data: Vec<u8>,
    shelves: Vec<Shelf>,
    /// 重排保真用条目表(M1 字形量级百级,克隆成本可忽略)
    entries: Vec<(GlyphBitmap, Rect)>,
    version: u64,
}

impl GlyphAtlas {
    pub fn new(width: u32, height: u32) -> Self {
        let (width, height) = (width.max(1), height.max(1));
        Self {
            width,
            height,
            data: vec![0; (width * height) as usize],
            shelves: Vec::new(),
            entries: Vec::new(),
            version: 0,
        }
    }

    pub fn width(&self) -> u32 { self.width }
    pub fn height(&self) -> u32 { self.height }
    pub fn texture(&self) -> &[u8] { &self.data }
    pub fn version(&self) -> u64 { self.version }

    pub fn insert(&mut self, bmp: &GlyphBitmap) -> Rect {
        loop {
            if let Some(rect) = self.try_place(bmp.width, bmp.height) {
                self.blit(bmp, rect);
                self.entries.push((bmp.clone(), rect));
                return rect;
            }
            self.grow();
        }
    }

    fn try_place(&mut self, w: u32, h: u32) -> Option<Rect> {
        if w > self.width {
            return None; // 单字形超宽:由 grow 加宽处理
        }
        for shelf in &mut self.shelves {
            if h <= shelf.height && shelf.used_x + w <= self.width {
                let r = Rect { u: shelf.used_x, v: shelf.y, w, h };
                shelf.used_x += w;
                return Some(r);
            }
        }
        let last_bottom = self.shelves.last().map_or(0, |s| s.y + s.height);
        if last_bottom + h <= self.height {
            self.shelves.push(Shelf { y: last_bottom, height: h, used_x: w });
            return Some(Rect { u: 0, v: last_bottom, w, h });
        }
        None
    }

    fn grow(&mut self) {
        // 高度倍增;单字形超宽时宽度也提到能容纳
        self.height = self.height.saturating_mul(2).max(1);
        self.data = vec![0; (self.width * self.height) as usize];
        self.shelves.clear();
        self.version += 1;
        let entries = std::mem::take(&mut self.entries);
        for (bmp, _) in entries {
            let rect = self
                .try_place(bmp.width, bmp.height)
                .expect("doubled atlas must fit prior entries");
            self.blit(&bmp, rect);
            self.entries.push((bmp, rect));
        }
    }

    fn blit(&mut self, bmp: &GlyphBitmap, r: Rect) {
        for y in 0..r.h {
            let dst = ((r.v + y) * self.width + r.u) as usize;
            let src = (y * bmp.width) as usize;
            self.data[dst..dst + r.w as usize]
                .copy_from_slice(&bmp.pixels[src..src + r.w as usize]);
        }
    }
}
```

- [ ] **Step 4: 全绿 + 提交**

Run: `cargo test -p mica-render font::atlas && cargo fmt --all && cargo clippy -p mica-render --all-targets -- -D warnings`

```bash
git add crates/mica-render/
git commit -m "feat(font): shelf-packed glyph atlas with growth repack"
```

---

### Task 4: 64B CellInstance + shader v2 + 纹理上传改造

**Files:**
- Modify: `crates/mica-render/src/frame.rs`(CellInstance 64B)
- Rewrite: `crates/mica-render/src/cells.wgsl`
- Modify: `crates/mica-render/src/pipeline.rs`(顶点布局 4×Float32x4;`set_atlas(&mut self, atlas: &GlyphAtlas)` 按 version 重传;Globals 缩为 viewport+pad)
- Delete: `crates/mica-render/src/atlas.rs`(v1;`lib.rs` 去掉 `pub mod atlas;`)

**Interfaces:**
- Consumes: `font::atlas::{GlyphAtlas, Rect}`(T3)
- Produces:
  - `frame::CellInstance { pos_uv: [f32;4] /*x,y,u,v*/, size_uv: [f32;4] /*w,h,uw,uvh*/, fg: [f32;4], bg: [f32;4] }`(64B,`#[repr(C)]` Pod)
  - `frame::blank_instance(x_px, y_px, w_px, h_px, fg, bg) -> CellInstance`(空格/占位:uv 尺寸 0,shader 采样恒 0 墨)
  - `Renderer::set_atlas(&mut self, atlas: &GlyphAtlas)`(version 变化才重传;UV 换算 `u/ atlas.width` 在 CPU 侧做,纹理无 padding)

- [ ] **Step 1: 改 frame.rs 布局测试(红)**

```rust
    #[test]
    fn instance_layout_is_pod_64() {
        assert_eq!(std::mem::size_of::<CellInstance>(), 64);
        assert_eq!(std::mem::offset_of!(CellInstance, pos_uv), 0);
        assert_eq!(std::mem::offset_of!(CellInstance, size_uv), 16);
        assert_eq!(std::mem::offset_of!(CellInstance, fg), 32);
        assert_eq!(std::mem::offset_of!(CellInstance, bg), 48);
    }
```

(其余 frame 测试在 T5 重建;本任务先让布局测试红。)

- [ ] **Step 2: CellInstance v2**

```rust
/// One drawable cell. `pos_uv = [x_px, y_px, u, v]`(uv 为图集内左上,0..1),
/// `size_uv = [w_px, h_px, uw, uvh]`(像素尺寸 + uv 尺寸,0..1)。
/// 空白格(空格/spacer)用 `blank_instance`:uv 尺寸 0,shader 墨恒 0。
#[repr(C)]
#[derive(Clone, Copy, bytemuck::Pod, bytemuck::Zeroable)]
pub struct CellInstance {
    pub pos_uv: [f32; 4],
    pub size_uv: [f32; 4],
    pub fg: [f32; 4],
    pub bg: [f32; 4],
}
```

- [ ] **Step 3: cells.wgsl v2(完整最终版;空白格的墨量由 vs 编码到 location(3))**

```wgsl
// Instanced quads, one per cell. Instance: 4 × vec4f (64B stride):
//   (0) [x_px, y_px, u, v]  (1) [w_px, h_px, uw, uvh]
//   (2) fg.rgb,_            (3) bg.rgb,_
// 空白格(空格/spacer)uv 尺寸为 0:vs 输出 ink_mask=0,fragment 不采样出墨。
const CORNERS = array<vec2f, 6>(
    vec2f(0.0, 0.0), vec2f(1.0, 0.0), vec2f(0.0, 1.0),
    vec2f(0.0, 1.0), vec2f(1.0, 0.0), vec2f(1.0, 1.0),
);

struct Globals {
    viewport: vec2f,
    _pad: vec2f,
};
@group(0) @binding(0) var<uniform> globals: Globals;
@group(0) @binding(1) var glyphs: texture_2d<f32>;
@group(0) @binding(2) var glyph_sampler: sampler;

struct VsOut {
    @builtin(position) pos: vec4f,
    @location(0) uv: vec2f,
    @location(1) fg: vec3f,
    @location(2) bg: vec3f,
    @location(3) ink_mask: f32,
};

@vertex
fn vs(
    @builtin(vertex_index) vi: u32,
    @location(0) pos_uv: vec4f,
    @location(1) size_uv: vec4f,
    @location(2) fg: vec4f,
    @location(3) bg: vec4f,
) -> VsOut {
    let corner = CORNERS[vi];
    let x = pos_uv.x + corner.x * size_uv.x;
    let y = pos_uv.y + corner.y * size_uv.y;
    var out: VsOut;
    out.pos = vec4f(x / globals.viewport.x * 2.0 - 1.0, 1.0 - y / globals.viewport.y * 2.0, 0.0, 1.0);
    out.uv = pos_uv.zw + corner * size_uv.zw;
    out.fg = fg.rgb;
    out.bg = bg.rgb;
    out.ink_mask = select(0.0, 1.0, size_uv.z > 0.0);
    return out;
}

@fragment
fn fs(inp: VsOut) -> @location(0) vec4f {
    let ink = textureSample(glyphs, glyph_sampler, inp.uv).r * inp.ink_mask;
    return vec4f(mix(inp.bg, inp.fg, ink), 1.0);
}
```

- [ ] **Step 4: pipeline.rs 改造**

- `VertexBufferLayout`:stride 64,四个 `Float32x4` @ offset 0/16/32/48,shader_location 0/1/2/3
- `Globals` 缩为 `[viewport, pad]`(32B uniform 不变)
- 删除构造期 `Atlas::ascii()` 上传;新增:

```rust
    /// 图集更新(version 变化才重传;首次调用建立纹理)。
    pub fn set_atlas(&mut self, atlas: &crate::font::atlas::GlyphAtlas) {
        if self.atlas_version == atlas.version() && self.atlas_texture.is_some() {
            return;
        }
        self.atlas_version = atlas.version();
        let size = wgpu::Extent3d { width: atlas.width(), height: atlas.height(), depth_or_array_layers: 1 };
        let texture = self.device.create_texture(&wgpu::TextureDescriptor {
            label: Some("glyph atlas"),
            size,
            mip_level_count: 1,
            sample_count: 1,
            dimension: wgpu::TextureDimension::D2,
            format: wgpu::TextureFormat::R8Unorm,
            usage: wgpu::TextureUsages::COPY_DST | wgpu::TextureUsages::TEXTURE_BINDING,
            view_formats: &[],
        });
        self.queue.write_texture(
            texture.as_image_copy(),
            atlas.texture(),
            wgpu::TexelCopyBufferLayout { offset: 0, bytes_per_row: Some(atlas.width()), rows_per_image: Some(atlas.height()) },
            size,
        );
        let view = texture.create_view(&wgpu::TextureViewDescriptor::default());
        let sampler = self.device.create_sampler(&wgpu::SamplerDescriptor {
            label: Some("glyphs"),
            mag_filter: wgpu::FilterMode::Linear, // 灰度 AA 需要
            min_filter: wgpu::FilterMode::Linear,
            ..wgpu::SamplerDescriptor::default()
        });
        // 重建 bind_group(新 view/sampler),字段存 texture 供 drop
        self.bind_group = self.rebuild_bind_group(&view, &sampler);
        self.atlas_texture = Some(texture);
    }
```

(字段 `atlas_version: u64`、`atlas_texture: Option<wgpu::Texture>`;`rebuild_bind_group` 抽出原 bind_group 构造段;`bytes_per_row = width` 合法——write_texture 对 1 字节纹素允许非 4 对齐行距?**核实点:若报对齐错,改为 `div_ceil(256)*256` padded 上传**——实施时以编译/运行错误为准修正,并在此文件留注释。)
- draw() 逻辑不变(实例缓冲路径照旧)

- [ ] **Step 5: 编译+布局测试绿;frame 其余测试本任务允许临时删除(T5 重建)——`build_instances` 暂留旧签名会编译错,直接把旧 `build_instances` 函数体替换为 `todo!("rebuilt in T5")` 并删除其旧测试,app.rs 调用处同注释;真机不可运行状态,可接受(一个提交内的中间态,任务完成时整体可用)**

Run: `cargo test -p mica-render instance_layout && cargo fmt --all && cargo clippy -p mica-render --all-targets -- -D warnings`
(注:app.rs 因 todo! 不可运行但可编译;跨平台 clippy 同过。)

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(render): 64B instances with atlas uv rects; shader v2; dynamic atlas upload"
```

---

### Task 5: GlyphRouter + build_instances v2(宽字符/样式/反色/光标)

**Files:**
- Create: `crates/mica-render/src/font/router.rs`
- Modify: `crates/mica-render/src/font/mod.rs`(追加 `pub mod router;`)
- Modify: `crates/mica-render/src/frame.rs`(build v2;恢复并扩写测试)
- Modify: `crates/mica-app/src/app.rs`(`build_instances` 调用改新签名,`todo!` 换成 FakeRouter 占位实现——交叉编译过即可,真路由 T6/T7 接)

**Interfaces:**
- Consumes: T1 `GlyphStyle`/`char_width`、T2 `FontMetrics`、T3 `GlyphAtlas`/`Rect`、T4 `CellInstance`
- Produces:
  - `font::router::GlyphInfo { uv: [f32;4], size_px: [f32;2], offset_px: [f32;2] }`(uv=(u,v,uw,uvh) 已归一化;offset = 相对格左上角的字形偏移)
  - `font::router::GlyphRouter` trait:

```rust
pub trait GlyphRouter {
    /// 路由一个字符+样式到图集条目;同 (char, style) 必须命中缓存同值。
    /// 空格与未知字符由实现者决定(通常路由到空白或 .notdef)。
    fn route(&mut self, ch: char, style: GlyphStyle) -> GlyphInfo;
}
```

  - `frame::build_instances(surface: &Surface, router: &mut dyn GlyphRouter, metrics: &FontMetrics) -> Vec<CellInstance>`
  - 语义(**T7 冒烟的验收依据**):每格一个实例;`WIDE_CHAR_SPACER` 与空格 → `blank_instance`(bg 正常);`WIDE_CHAR` 正常 route(字形天然 2 格宽);INVERSE 与光标交换保留(T2 后既有行为);字形绘制位置 = 格左上 + `offset_px`,尺寸 = `size_px`

- [ ] **Step 1: 写失败测试(FakeRouter)**

`frame.rs` 测试模块:

```rust
    /// 可编程假路由:每个字符回固定字形,便于断言几何。
    struct FakeRouter {
        atlas_w: f32,
        atlas_h: f32,
        glyph_w: f32,
        glyph_h: f32,
        wide: fn(char) -> bool,
    }
    impl crate::font::router::GlyphRouter for FakeRouter {
        fn route(&mut self, ch: char, _style: crate::font::GlyphStyle) -> crate::font::router::GlyphInfo {
            let (w, adv) = if (self.wide)(ch) { (self.glyph_w * 2.0, 2.0) } else { (self.glyph_w, 1.0) };
            let _ = adv;
            crate::font::router::GlyphInfo {
                uv: [0.25, 0.25, w / self.atlas_w, self.glyph_h / self.atlas_h],
                size_px: [w, self.glyph_h],
                offset_px: [1.0, 2.0],
            }
        }
    }
    fn metrics_8x16() -> crate::font::metrics::FontMetrics {
        crate::font::metrics::FontMetrics { cell_width: 8.0, line_height: 16.0, ascent: 12.0, descent: 4.0 }
    }

    #[test]
    fn every_cell_yields_instance_with_background() {
        let s = Surface::new(ScreenSize::new(4, 2));
        let mut r = FakeRouter { atlas_w: 64.0, atlas_h: 64.0, glyph_w: 6.0, glyph_h: 12.0, wide: |_| false };
        assert_eq!(build_instances(&s, &mut r, &metrics_8x16()).len(), 8);
    }

    #[test]
    fn typed_glyph_uses_router_geometry() {
        let mut s = Surface::new(ScreenSize::new(4, 1));
        s.feed(b"A");
        let mut r = FakeRouter { atlas_w: 64.0, atlas_h: 64.0, glyph_w: 6.0, glyph_h: 12.0, wide: |_| false };
        let inst = build_instances(&s, &mut r, &metrics_8x16());
        // 格左上 (0,0) + offset (1,2);尺寸 6x12;uv 来自路由
        assert_eq!(inst[0].pos_uv, [1.0, 2.0, 0.25, 0.25]);
        assert_eq!(inst[0].size_uv, [6.0, 12.0, 6.0 / 64.0, 12.0 / 64.0]);
        // 第二格空格:blank——尺寸 = 格尺寸,uv 尺寸 0
        assert_eq!(inst[1].size_uv[0], 8.0);
        assert_eq!(inst[1].size_uv[1], 16.0);
        assert_eq!(inst[1].size_uv[2], 0.0);
    }

    #[test]
    fn wide_char_routes_wide_spacer_blank() {
        let mut s = Surface::new(ScreenSize::new(4, 1));
        s.feed("中"); // WIDE_CHAR + SPACER(alacritty 网格语义)
        let mut r = FakeRouter { atlas_w: 64.0, atlas_h: 64.0, glyph_w: 6.0, glyph_h: 12.0, wide: |_| true };
        let inst = build_instances(&s, &mut r, &metrics_8x16());
        assert_eq!(inst[0].size_uv[0], 12.0, "宽字形横跨 2 格");
        assert_eq!(inst[1].size_uv[2], 0.0, "spacer 格为空白背景");
        assert_eq!(inst[1].size_uv[0], 8.0);
    }

    #[test]
    fn bold_style_routed_and_inverse_cursor_kept() {
        let mut s = Surface::new(ScreenSize::new(4, 1));
        s.feed(b"\x1b[1mA\x1b[m\x1b[7mB");
        let mut seen_styles = std::cell::RefCell::new(Vec::new());
        struct RecordingRouter<'a> {
            seen: &'a std::cell::RefCell<Vec<crate::font::GlyphStyle>>,
            inner: FakeRouter,
        }
        impl crate::font::router::GlyphRouter for RecordingRouter<'_> {
            fn route(&mut self, ch: char, style: crate::font::GlyphStyle) -> crate::font::router::GlyphInfo {
                self.seen.borrow_mut().push(style);
                self.inner.route(ch, style)
            }
        }
        let mut r = RecordingRouter {
            seen: &seen_styles,
            inner: FakeRouter { atlas_w: 64.0, atlas_h: 64.0, glyph_w: 6.0, glyph_h: 12.0, wide: |_| false },
        };
        let inst = build_instances(&s, &mut r, &metrics_8x16());
        // 样式确实到达路由:两个非空格字形,第一个带 bold
        assert_eq!(*seen_styles.borrow(), vec![
            crate::font::GlyphStyle { bold: true, italic: false },
            crate::font::GlyphStyle::PLAIN,
        ]);
        // B(索引 1)反色:fg 暗底、bg 白;光标此时在 (2,0):空格格二次交换成 fg 白 bg 暗
        assert_eq!(inst[1].fg, [0x1e as f32 / 255.0, 0x1e as f32 / 255.0, 0x1e as f32 / 255.0, 0.0]);
        assert_eq!(inst[1].bg, [1.0, 1.0, 1.0, 0.0]);
        assert_eq!(inst[2].fg, [1.0, 1.0, 1.0, 0.0]);
        assert_eq!(inst[2].bg, [0x1e as f32 / 255.0, 0x1e as f32 / 255.0, 0x1e as f32 / 255.0, 0.0]);
    }
}
```

(最后一个测试的数组断言按现有写法展开为逐元素比较,执行者参照既有 `sgr_reverse_video_swaps_fg_bg` 的断言风格补全;**样式的路由验证**用"FakeRouter 记录 style 断言后清空"模式,在测试内用 `RefCell<Vec<GlyphStyle>>` 捕获。)

- [ ] **Step 2: 跑红 → Step 3: 实现 router.rs + frame v2**

`router.rs`:

```rust
//! 字形路由:字符+样式 → 图集几何。DirectWrite 实现见 dwrite.rs;
//! 测试用 Fake(见 frame::tests)。

use super::atlas::{GlyphAtlas, GlyphBitmap};
use super::GlyphStyle;

/// 一个已就位的字形:uv 已归一化(0..1),offset 相对格左上。
#[derive(Debug, Clone, Copy)]
pub struct GlyphInfo {
    pub uv: [f32; 4],
    pub size_px: [f32; 2],
    pub offset_px: [f32; 2],
}

pub trait GlyphRouter {
    fn route(&mut self, ch: char, style: GlyphStyle) -> GlyphInfo;
}
```

(空白实例在 frame 侧直接构造,不占图集、不经路由——router 无 blank 概念。)

`frame.rs` build v2 核心:

```rust
pub fn build_instances(
    surface: &Surface,
    router: &mut dyn GlyphRouter,
    metrics: &FontMetrics,
) -> Vec<CellInstance> {
    let grid = surface.grid();
    let cols = grid.columns();
    let rows = grid.screen_lines();
    let cursor = grid.cursor.point;
    let cursor_line = usize::try_from(cursor.line.0).unwrap_or(usize::MAX);
    let cursor_col = cursor.column.0;
    let mut out = Vec::with_capacity(cols * rows);
    for line in 0..rows {
        for col in 0..cols {
            let cell = &grid[Line(line as i32)][Column(col)];
            let mut fg = resolve(cell.fg, DEFAULT_FG, DEFAULT_BG);
            let mut bg = resolve(cell.bg, DEFAULT_FG, DEFAULT_BG);
            // 先反色后光标(双交换抵消,光标在选区仍可辨——已裁定顺序)
            if cell.flags.contains(Flags::INVERSE) {
                std::mem::swap(&mut fg, &mut bg);
            }
            if line == cursor_line && col == cursor_col {
                std::mem::swap(&mut fg, &mut bg);
            }
            let x = col as f32 * metrics.cell_width;
            let y = line as f32 * metrics.line_height;
            let inst = if cell.flags.contains(Flags::WIDE_CHAR_SPACER) || cell.c == ' ' {
                blank(x, y, metrics, fg, bg)
            } else {
                let g = router.route(cell.c, GlyphStyle::from_flags(cell.flags));
                CellInstance {
                    pos_uv: [x + g.offset_px[0], y + g.offset_px[1], g.uv[0], g.uv[1]],
                    size_uv: [g.size_px[0], g.size_px[1], g.uv[2], g.uv[3]],
                    fg: to_color(fg),
                    bg: to_color(bg),
                }
            };
            out.push(inst);
        }
    }
    out
}
```

(`to_color`/`blank` 为文件内小助手,把 M0 的 `CellInstance::new` 逻辑拆两半;颜色换算与 M0 相同 u8/255。)

- [ ] **Step 4: 全绿 + 交叉验证 + 提交**

Run: `cargo test -p mica-render && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo clippy -p mica-app --target x86_64-pc-windows-msvc --all-targets -- -D warnings`

```bash
git add -A
git commit -m "feat(render): glyph routing + wide/spacer/style-aware instance building"
```

---

### Task 6: dwrite.rs——DirectWrite 光栅化路由(#[cfg(windows)])

**Files:**
- Create: `crates/mica-render/src/font/dwrite.rs`
- Modify: `crates/mica-render/src/font/mod.rs`(`#[cfg(windows)] pub mod dwrite;`)
- Modify: `crates/mica-render/Cargo.toml`(`[target.'cfg(windows)'.dependencies]` windows + feature)

**Interfaces:**
- Consumes: T1-T5 全部(font::{GlyphStyle, DEFAULT_FAMILIES, metrics::{FontMetrics}, atlas::*, router::*})
- Produces:
  - `font::dwrite::DwriteRouter::new(font_size_pt: f32, families: &[&str]) -> anyhow::Result<Self>`(家族缺失的链项跳过,全缺报错;附 `families_in_use() -> Vec<String>` 供日志)
  - `DwriteRouter::metrics(&self) -> FontMetrics`
  - `impl GlyphRouter for DwriteRouter`(缓存 (char, style-bits) → GlyphInfo;空白字符路由到空白 info 不占图集)
  - `DwriteRouter::atlas(&self) -> &GlyphAtlas` + `atlas_version()`(app 每帧比对触发 `Renderer::set_atlas`)

**测试说明**:真光栅化无法在 mac/CI 运行,本任务验证 = 交叉 clippylint 通过 + 真机冒烟(T7)。几何数学(归一化/offset)已在上游任务测试;此处仅接线。

- [ ] **Step 1: Cargo.toml**

```toml
[target.'cfg(windows)'.dependencies]
windows = { workspace = true, features = [
    "Win32_Graphics_DirectWrite",
] }
```

(workspace 里 windows 尚未有——根 Cargo.toml `[workspace.dependencies]` 加 `windows = "0.62"`;mica-app 侧原有 target 依赖改用 workspace 版,features 各自追加,**合并去重由 cargo 处理**。)

- [ ] **Step 2: 实现(核心骨架,签名全部来自核实报告)**

```rust
//! DirectWrite 光栅化路由。仅 Windows;签名经 2026-09-22 核实(windows 0.62.2)。

use std::collections::HashMap;

use super::atlas::{GlyphAtlas, GlyphBitmap};
use super::metrics::FontMetrics;
use super::router::{GlyphInfo, GlyphRouter};
use super::GlyphStyle;

use windows::core::{Interface, Param};
use windows::Win32::Graphics::DirectWrite::{
    DWriteCreateFactory, IDWriteFactory, IDWriteFont, IDWriteFontCollection, IDWriteFontFace,
    DWRITE_FACTORY_TYPE_SHARED, DWRITE_FONT_METRICS, DWRITE_FONT_STRETCH_NORMAL,
    DWRITE_FONT_STYLE, DWRITE_FONT_STYLE_ITALIC, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_WEIGHT,
    DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_WEIGHT_NORMAL, DWRITE_GLYPH_METRICS, DWRITE_GLYPH_OFFSET,
    DWRITE_GLYPH_RUN, DWRITE_MATRIX, DWRITE_MEASURING_MODE_NATURAL, DWRITE_RENDERING_MODE,
    DWRITE_RENDERING_MODE_DEFAULT, DWRITE_TEXTURE_ALIASED_1x1, DWRITE_TEXTURE_TYPE,
};

/// 一个家族的三个样式面(plain/bold/italic;bold-italic 复用 bold 面以简化,
/// M2 若需要再加)。HasCharacter 在家族任一面查。
struct FamilyFaces {
    name: String,
    plain: IDWriteFont,
    bold: IDWriteFont,
    italic: IDWriteFont,
    face: IDWriteFontFace, // plain 的 face,度量与宽度来源
    metrics: DWRITE_FONT_METRICS,
    max_advance_px: f32, // 'M' 的 advance(等宽字体即 cell 宽)
}

pub struct DwriteRouter {
    families: Vec<FamilyFaces>,
    em_size_dip: f32,
    ascent_px: f32,
    metrics: FontMetrics,
    atlas: GlyphAtlas,
    cache: HashMap<(char, u8), GlyphInfo>,
}

fn style_bits(s: GlyphStyle) -> u8 { u8::from(s.bold) | (u8::from(s.italic) << 1) }

impl DwriteRouter {
    pub fn new(font_size_pt: f32, families: &[&str]) -> anyhow::Result<Self> {
        let factory: IDWriteFactory = unsafe { DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED)? };
        let mut collection: Option<IDWriteFontCollection> = None;
        unsafe { factory.GetSystemFontCollection(&mut collection, true)? };
        let collection = collection.ok_or_else(|| anyhow::anyhow!("no system font collection"))?;

        let em_size_dip = font_size_pt * 96.0 / 72.0;
        let mut resolved = Vec::new();
        for name in families {
            let wide: windows::core::HSTRING = (*name).into();
            let mut index = 0u32;
            let mut exists = windows::core::BOOL::default();
            unsafe { collection.FindFamilyName(&wide, &mut index, &mut exists)? };
            if !exists.as_bool() {
                continue; // 家族缺失 → 跳过(回退链语义)
            }
            let family = unsafe { collection.GetFontFamily(index)? };
            let pick = |weight: DWRITE_FONT_WEIGHT, style: DWRITE_FONT_STYLE| unsafe {
                family.GetFirstMatchingFont(weight, DWRITE_FONT_STRETCH_NORMAL, style)
            };
            let plain = pick(DWRITE_FONT_WEIGHT_NORMAL, DWRITE_FONT_STYLE_NORMAL)?;
            let bold = pick(DWRITE_FONT_WEIGHT_BOLD, DWRITE_FONT_STYLE_NORMAL)?;
            let italic = pick(DWRITE_FONT_WEIGHT_NORMAL, DWRITE_FONT_STYLE_ITALIC)?;
            let face = unsafe { plain.CreateFontFace()? };
            let mut metrics = DWRITE_FONT_METRICS::default();
            unsafe { plain.GetMetrics(&mut metrics) };
            // 等宽判定用的 'M' advance
            let m_idx = [unsafe { glyph_index_for(&face, 'M')? }];
            let mut gm = [DWRITE_GLYPH_METRICS::default(); 1];
            unsafe {
                face.GetDesignGlyphMetrics(m_idx.as_ptr(), 1, gm.as_mut_ptr(), false)?;
            }
            let dpuem = metrics.designUnitsPerEm.max(1) as f32;
            let max_advance_px = gm[0].advanceWidth as f32 * em_size_dip / dpuem;
            let ascent_px = metrics.ascent as f32 * em_size_dip / dpuem;
            resolved.push(FamilyFaces {
                name: name.to_string(),
                plain,
                bold,
                italic,
                face,
                metrics,
                max_advance_px,
                ascent_px,
            });
        }
        if resolved.is_empty() {
            anyhow::bail!("no font family resolved from {families:?}");
        }
        let m0 = &resolved[0];
        let metrics = FontMetrics::from_dwrite(
            m0.metrics.ascent,
            m0.metrics.descent,
            m0.metrics.lineGap,
            m0.metrics.designUnitsPerEm,
            em_size_dip,
            m0.max_advance_px_du(), // 见下:FamilyFaces 存 design-unit advance 更稳
        );
        Ok(Self {
            families: resolved,
            em_size_dip,
            ascent_px: m0.ascent_px,
            metrics,
            atlas: GlyphAtlas::new(256, 256),
            cache: HashMap::new(),
        })
    }

    pub fn metrics(&self) -> FontMetrics { self.metrics }
    pub fn atlas(&self) -> &GlyphAtlas { &self.atlas }
    pub fn families_in_use(&self) -> Vec<String> {
        self.families.iter().map(|f| f.name.clone()).collect()
    }

    fn family_for(&self, ch: char) -> Option<&FamilyFaces> {
        self.families
            .iter()
            .find(|f| unsafe { f.plain.HasCharacter(ch as u32).map(|b| b.as_bool()).unwrap_or(false) })
    }
}

fn glyph_index_for(face: &IDWriteFontFace, ch: char) -> anyhow::Result<u16> {
    let mut code = [ch as u32];
    let mut idx = [0u16];
    unsafe { face.GetGlyphIndicesW(&mut code, &mut idx)? }; // 返回 u32 HRESULT→Result
    Ok(idx[0])
}

impl GlyphRouter for DwriteRouter {
    fn route(&mut self, ch: char, style: GlyphStyle) -> GlyphInfo {
        let key = (ch, style_bits(style));
        if let Some(g) = self.cache.get(&key) {
            return *g;
        }
        let info = self.rasterize(ch, style).unwrap_or_else(|| blank_fallback(ch, &self.metrics));
        self.cache.insert(key, info);
        info
    }
}

impl DwriteRouter {
    fn rasterize(&mut self, ch: char, style: GlyphStyle) -> Option<GlyphInfo> {
        if ch == ' ' || ch == '\u{0}' {
            return Some(GlyphInfo { uv: [0.0; 4], size_px: [0.0, 0.0], offset_px: [0.0, 0.0] });
        }
        let family = self.family_for(ch)?;
        let font: &IDWriteFont = match (style.bold, style.italic) {
            (true, _) => &family.bold,
            (false, true) => &family.italic,
            _ => &family.plain,
        };
        let face = unsafe { font.CreateFontFace().ok()? };
        let glyph = unsafe { glyph_index_for(&face, ch).ok()? };
        if glyph == 0 {
            return None; // .notdef → blank
        }
        let em = self.em_size_dip;
        let run = DWRITE_GLYPH_RUN {
            fontFace: std::mem::ManuallyDrop::new(Some(face)),
            fontEmSize: em,
            glyphCount: 1,
            glyphIndices: &glyph,
            glyphAdvances: &em, // 单字形 advance 由度量侧算,这里给 1em 占位(分析器只用索引渲染)
            glyphOffsets: &DWRITE_GLYPH_OFFSET { advanceOffset: 0.0, ascenderOffset: 0.0 },
            isSideways: windows::core::BOOL::default(),
            bidiLevel: 0,
        };
        // unsafe: factory 在 new 后未存——修正:Self 存 factory 字段(执行者补 factory: IDWriteFactory 字段并在此使用)
        let analysis = unsafe {
            self.factory
                .CreateGlyphRunAnalysis(&run, 1.0, None, DWRITE_RENDERING_MODE_DEFAULT, DWRITE_MEASURING_MODE_NATURAL, 0.0, self.ascent_px)
                .ok()?
        };
        let bounds = unsafe {
            analysis.GetAlphaTextureBounds(DWRITE_TEXTURE_ALIASED_1x1).ok()?
        };
        let (w, h) = ((bounds.right - bounds.left).max(0), (bounds.bottom - bounds.top).max(0));
        if w == 0 || h == 0 {
            return Some(GlyphInfo { uv: [0.0; 4], size_px: [0.0, 0.0], offset_px: [0.0, 0.0] });
        }
        let mut pixels = vec![0u8; (w * h) as usize];
        unsafe {
            analysis
                .CreateAlphaTexture(DWRITE_TEXTURE_ALIASED_1x1, &bounds, &mut pixels)
                .ok()?;
        }
        let rect = self.atlas.insert(&GlyphBitmap { width: w as u32, height: h as u32, pixels });
        let aw = self.atlas.width() as f32;
        let ah = self.atlas.height() as f32;
        let dpuem = family.metrics.designUnitsPerEm.max(1) as f32;
        // 字形相对格左上:水平 = bounds.left;垂直 = ascent + bounds.top(top 为负=基线上方)
        Some(GlyphInfo {
            uv: [rect.u as f32 / aw, rect.v as f32 / ah, rect.w as f32 / aw, rect.h as f32 / ah],
            size_px: [w as f32, h as f32],
            offset_px: [bounds.left as f32, self.ascent_px + bounds.top as f32],
        })
        // 注:advance(dpuem 换算)当前未用于实例(格子宽度由 metrics 决定),
        // 变宽字形自然越出 1 格的视觉由宽字符标志覆盖;`_ = dpuem` 抑制未用告警或删除该变量。
    }
}
```

**执行者注意(骨架里的已知待补点,以编译器为准修正而非照抄)**:
1. `Self` 需存 `factory: IDWriteFactory` 字段(骨架注释处);
2. `FamilyFaces` 的 `max_advance_px_du()` 未定义——直接存 `max_advance_du: u32`(design units),`FontMetrics::from_dwrite` 本就要 du;
3. `GetGlyphIndicesW` 签名未在核实报告里(`*mut u32/*mut u16` 或 slice 型)——**编译器为准**;若形态不符改用返回值形式;
4. 骨架中与类型不符的小笔误以编译器为准修正(勿照抄编译不过的行);
5. `DWRITE_RENDERING_MODE_DEFAULT` 常量名若不存在用报告未覆盖项里最接近的 `DWRITE_RENDERING_MODE_NATURAL`(先试 DEFAULT);
6. unused import(`Interface`/`Param`/`DWRITE_MATRIX`/`DWRITE_TEXTURE_TYPE` 等)按 clippy 清理。

- [ ] **Step 3: 交叉验证(mac)**

Run: `cargo clippy -p mica-render --target x86_64-pc-windows-msvc --all-targets -- -D warnings && cargo test -p mica-render`
Expected: 双绿(mac 测试不含 dwrite 路径)。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat(font): dwrite rasterizing glyph router (windows)"
```

---

### Task 7: app 接线 + M1a/M1b 真机冒烟

**Files:**
- Modify: `crates/mica-app/src/app.rs`(FakeRouter 占位 → DwriteRouter;窗口尺寸/网格换算用真度量;每帧 `set_atlas` 比对)

**Interfaces:**
- Consumes: T6 `DwriteRouter`(windows)、T5 `build_instances(surface, router, metrics)`、T4 `Renderer::set_atlas`、T2 `Surface::set_cell_metrics`
- Produces: 真机上 DirectWrite 渲染的终端;M1a+M1b 验收

- [ ] **Step 1: 接线(windows 分支)**

`app.rs` 关键改动(执行处按现状代码融合):

```rust
// init_terminal 内(windows):
let mut router = mica_render::font::dwrite::DwriteRouter::new(12.0, mica_render::font::DEFAULT_FAMILIES)
    .expect("no fonts resolved");
eprintln!("font families in use: {:?}", router.families_in_use()); // 冒烟期观察回退结果
let metrics = router.metrics();
surface.set_cell_metrics(metrics.cell_width as u16, metrics.line_height as u16);
// 窗口初始 100x30 格(替换 80x24 常量换算):
// width = 100.0 * metrics.cell_width 等,AdjustWindowRect 前取整
// Terminal 结构体加 router 字段
```

`draw_frame`(最终形态;`Terminal` 新增字段 `router: DwriteRouter`、`metrics: FontMetrics`、`renderer_atlas_version: u64`):

```rust
fn draw_frame() {
    STATE.with(|cell| {
        let Some(t) = cell.borrow_mut().as_mut() else { return };
        let instances = build_instances(&t.term, &mut t.router, &t.metrics);
        let version = t.router.atlas().version();
        if t.renderer_atlas_version != version {
            t.renderer.set_atlas(t.router.atlas());
            t.renderer_atlas_version = version;
        }
        t.renderer.draw(&t.wgpu_surface, &t.config, &instances);
    })
}
```

resize:`WM_SIZE` 换算改 `metrics.cell_width/line_height`;退出流程不变。

- [ ] **Step 2: mac 交叉验证**

Run: `cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo clippy -p mica-app --target x86_64-pc-windows-msvc --all-targets -- -D warnings`

- [ ] **Step 3: 真机冒烟(在你的 Windows 机器上,全部通过才算 M1a+M1b 完成)**

```bash
git pull && cargo run --bin mica
```

1. 字体明显是 DirectWrite 抗锯齿渲染(非像素块);观察 stderr 的 families 列表:装了 Sarasa 应见其居首,未装则 Cascadia
2. `echo 你好世界 Hello World` 中文正常显示(不再是 `?`),中英混排基线对齐
3. `dir` 输出中文文件名正常;`cls` 后重打
4. 中文全角标点(`。,`)占 2 格,与后续字符不重叠
5. `[Console]::Write("`e[1mBold`e[m normal")` 加粗字形明显变粗(走 bold 面)
6. 选中一段文字(PSReadLine 反色)正常反色;光标块正常
7. 窗口 resize:网格按字体度量重排,无错位
8. emoji `😀` 显示为灰度形状(彩色是 M2,声明过的边界)
9. `cargo test --workspace` 双平台 CI 绿(推送后)

- [ ] **Step 4: Commit + push**

```bash
git add -A
git commit -m "feat(app): wire dwrite router, metrics-driven grid; M1a/M1b smoke items"
git push
```

---

## Self-Review 记录(已执行)

- 覆盖:spec §3 全部(chain 在 T1/T6、metrics T2、dwrite T6、atlas T3)与 §4 的 64B/宽字符/样式(T4/T5);调色板/配置/DECCKM/事件化/脏区明确划归计划 B ✓
- 占位扫描:T4 Step3/T5 Step1/T6/T7 中标注"执行者注意/以编译器为准"的是**对未核实签名(DirectWrite 运行时细节)的诚实标注**,并给出了确定的处理规则(编译器/真机为准),非 TBD ✓
- 类型一致性:GlyphInfo 三处(定义/T5 测试/T6 使用)、FontMetrics 流向、CellInstance 64B 全文一致 ✓
- 已知风险单列:write_texture 行距对齐(T4 核实点)、GetGlyphIndicesW 签名(T6 注意点 3)、渲染模式常量名(T6 注意点 5)
